<!DOCTYPE html>
<html lang="az">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yeni Koordinat Əlavə Et</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Custom Styles to Enhance Bootstrap */
        :root {
            --primary-dark: #212529;
            /* bg-dark */
            --secondary-card: #343a40;
            /* bg-secondary */
            --text-light: #f8f9fa;
            /* text-light */
            --accent-green: #28a745;
            /* btn-success */
            --accent-green-hover: #218838;
            --accent-blue: #007bff;
            /* for links/icons */
            --accent-blue-hover: #0056b3;
            --input-bg: #495057;
            /* Darker input background for contrast */
            --input-border: #6c757d;
            --input-focus-border: #80bdff;
            --input-focus-shadow: rgba(128, 189, 255, 0.25);
        }

        body {
            background-color: var(--primary-dark);
            color: var(--text-light);
            /* Flexbox for centering content */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            /* Ensures body takes full viewport height */
            padding: 20px;
            box-sizing: border-box;
            /* Include padding in element's total width and height */
            margin: 0; /* Remove default body margin */
        }

        .card {
            background-color: var(--secondary-card);
            border: none;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
            width: 100%;
            /* Adjusted max-width for better form layout */
        }

        .card-title {
            font-size: 2.2rem;
            font-weight: 700;
            margin-bottom: 25px;
            color: var(--accent-blue);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .form-label {
            font-weight: 600;
            color: var(--text-light);
            margin-bottom: 8px;
            font-size: 1.05rem;
        }

        .input-group-custom {
            position: relative;
            margin-bottom: 20px;
        }

        .input-group-custom .form-control {
            background-color: var(--input-bg);
            color: var(--text-light);
            border: 1px solid var(--input-border);
            padding: 12px 15px 12px 45px;
            /* Adjust padding for icon */
            border-radius: 10px;
            font-size: 1rem;
            transition: border-color 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease;
        }

        .input-group-custom .form-control::placeholder {
            color: rgba(248, 249, 250, 0.6);
            /* Lighter placeholder text */
        }

        .input-group-custom .form-control:focus {
            background-color: var(--input-bg);
            /* Keep background consistent on focus */
            border-color: var(--input-focus-border);
            box-shadow: 0 0 0 0.25rem var(--input-focus-shadow);
            color: var(--text-light);
        }

        .input-group-custom .input-icon {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--accent-blue);
            font-size: 1.1rem;
            pointer-events: none;
            /* Make icon unclickable */
        }

        .btn-submit {
            background-color: var(--accent-green);
            border-color: var(--accent-green);
            color: white;
            padding: 12px 25px;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 10px;
            transition: background-color 0.3s ease, transform 0.2s ease, border-color 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .btn-submit:hover {
            background-color: var(--accent-green-hover);
            border-color: var(--accent-green-hover);
            transform: translateY(-2px);
            color: white;
            /* Keep text white on hover */
        }

        .manage-link {
            display: block;
            text-align: center;
            margin-bottom: 25px;
            font-size: 1.1rem;
            color: var(--accent-blue);
            text-decoration: none;
            transition: color 0.2s ease, transform 0.2s ease;
            font-weight: 500;
        }

        .manage-link:hover {
            color: var(--accent-blue-hover);
            transform: translateY(-1px);
            text-decoration: underline;
        }

        /* Adjustments for Bootstrap's default values */
        .mb-3 {
            margin-bottom: 20px !important;
            /* Override default mb-3 to match custom input-group-custom */
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="card">
            <h2 class="card-title"><i class="fas fa-map-marker-alt"></i> Yeni Koordinat Əlavə Et</h2>

            <a href="manage-coordinates" class="manage-link">
                <i class="fas fa-edit"></i> Koordinatlara Düzəliş Ver
            </a>

            <form action="/api/coordinates" method="POST">
                <div class="input-group-custom">
                    <label for="addressName" class="form-label">Ünvan Adı:</label>
                    <i class="fas fa-home input-icon"></i>
                    <input type="text" id="addressName" name="addressName" class="form-control"
                        placeholder="Məsələn: Zaqatala Mərkəzi" required>
                </div>

                <div class="input-group-custom">
                    <label for="latitude" class="form-label">Enlik (Latitude):</label>
                    <i class="fas fa-globe-americas input-icon"></i>
                    <input type="text" id="latitude" name="latitude" class="form-control" value="41."
                        placeholder="Məsələn: 41.0000" required>
                </div>

                <div class="input-group-custom">
                    <label for="longitude" class="form-label">Uzunluq (Longitude):</label>
                    <i class="fas fa-globe-americas input-icon"></i>
                    <input type="text" id="longitude" name="longitude" class="form-control" value="46."
                        placeholder="Məsələn: 46.0000" required>
                </div>

                <button type="submit" class="btn btn-submit w-100">
                    <i class="fas fa-save"></i> Yadda Saxla
                </button>
            </form>
        </div>
    </div>

    <!-- Bootstrap JS (Optional, for components like dropdowns, tooltips) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>