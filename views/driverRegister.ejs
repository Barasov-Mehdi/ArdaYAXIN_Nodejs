<!DOCTYPE html>
<html lang="az">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sürücü Qeydiyyatı</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Global Variables */
        :root {
            --primary-bg: #f8f9fa; /* Light background */
            --secondary-bg: #ffffff; /* Card background */
            --accent-color: #007bff; /* Primary button/link color */
            --accent-hover: #0056b3; /* Primary button hover */
            --text-dark: #343a40; /* Dark text */
            --text-light: #6c757d; /* Lighter text for labels */
            --border-color: #dee2e6; /* Border color for inputs/cards */
            --shadow-light: 0 4px 8px rgba(0, 0, 0, 0.05); /* Light shadow */
            --focus-glow: rgba(0, 123, 255, 0.25); /* Focus glow for inputs */
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--primary-bg);
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            box-sizing: border-box;
        }

        .container {
            max-width: 500px;
            width: 100%;
            padding: 30px;
            background: var(--secondary-bg);
            border-radius: 12px;
            box-shadow: var(--shadow-light);
            border: 1px solid var(--border-color);
            box-sizing: border-box; /* Ensure padding doesn't increase total width */
        }

        h1 {
            text-align: center;
            color: var(--text-dark);
            margin-bottom: 30px;
            font-size: 28px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: var(--text-light);
            font-weight: 500;
            font-size: 0.95em;
        }

        .input-icon-group {
            position: relative;
        }

        .input-icon-group input {
            width: 100%;
            padding: 12px 12px 12px 40px; /* Left padding for icon */
            border: 1px solid var(--border-color);
            border-radius: 8px;
            font-size: 1em;
            color: var(--text-dark);
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
            box-sizing: border-box; /* Include padding in element's total width and height */
        }

        .input-icon-group input:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 0 0.25rem var(--focus-glow);
            outline: none;
        }

        .input-icon-group .icon {
            position: absolute;
            left: 12px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-light);
            font-size: 1.1em;
        }

        button[type="submit"] {
            width: 100%;
            padding: 12px;
            background-color: var(--accent-color);
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1.1em;
            font-weight: 600;
            transition: background-color 0.3s ease, transform 0.2s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        button[type="submit"]:hover {
            background-color: var(--accent-hover);
            transform: translateY(-1px);
        }

        /* Responsive adjustments */
        @media (max-width: 600px) {
            .container {
                margin: 20px auto;
                padding: 20px;
                border-radius: 8px;
            }

            h1 {
                font-size: 24px;
                margin-bottom: 25px;
            }

            button[type="submit"] {
                font-size: 1em;
                padding: 10px;
            }

            .input-icon-group input {
                padding: 10px 10px 10px 35px;
            }

            .input-icon-group .icon {
                left: 10px;
                font-size: 1em;
            }
        }

        @media (max-width: 400px) {
            .container {
                padding: 15px;
            }

            h1 {
                font-size: 20px;
            }
            label {
                font-size: 0.9em;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1><i class="fas fa-user-plus"></i> Sürücü Qeydiyyatı</h1>
        <form action="/api/drivers/register" method="POST">
            <div class="form-group">
                <label for="firstName">Ad</label>
                <div class="input-icon-group">
                    <i class="fas fa-signature icon"></i>
                    <input type="text" id="firstName" name="firstName" placeholder="Adınızı daxil edin" required>
                </div>
            </div>
            <div class="form-group">
                <label for="lastName">Soyad</label>
                <div class="input-icon-group">
                    <i class="fas fa-signature icon"></i>
                    <input type="text" id="lastName" name="lastName" placeholder="Soyadınızı daxil edin" required>
                </div>
            </div>
            <div class="form-group">
                <label for="phone">Telefon</label>
                <div class="input-icon-group">
                    <i class="fas fa-phone icon"></i>
                    <input type="tel" id="phone" name="phone" placeholder="Telefon nömrənizi daxil edin" required>
                </div>
            </div>
            <div class="form-group">
                <label for="carPlate">Maşın Nömrəsi</label>
                <div class="input-icon-group">
                    <i class="fas fa-car-side icon"></i>
                    <input type="text" id="carPlate" name="carPlate" placeholder="Maşın nömrəsini daxil edin (örn: 99-AA-999)" required>
                </div>
            </div>
            <div class="form-group">
                <label for="carModel">Maşın Modeli</label>
                <div class="input-icon-group">
                    <i class="fas fa-car icon"></i>
                    <input type="text" id="carModel" name="carModel" placeholder="Maşın modelini daxil edin" required>
                </div>
            </div>
            <div class="form-group">
                <label for="carColor">Maşın Rəngi</label>
                <div class="input-icon-group">
                    <i class="fas fa-palette icon"></i>
                    <input type="text" id="carColor" name="carColor" placeholder="Maşın rəngini daxil edin" required>
                </div>
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <div class="input-icon-group">
                    <i class="fas fa-envelope icon"></i>
                    <input type="email" id="email" name="email" placeholder="Email adresinizi daxil edin" required>
                </div>
            </div>
            <div class="form-group">
                <label for="password">Şifrə</label>
                <div class="input-icon-group">
                    <i class="fas fa-lock icon"></i>
                    <input type="password" id="password" name="password" placeholder="Şifrənizi daxil edin" required>
                </div>
            </div>
            <button type="submit"><i class="fas fa-plus-circle"></i> Sürücünü Əlavə Et</button>
        </form>
    </div>
</body>

</html>